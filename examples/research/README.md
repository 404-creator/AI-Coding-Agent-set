# Research Example

Generate instructions for a paper reproduction repository:

```bash
agent-set init --profile research --skills paper-reader,data-analysis,test-writer --output .
```

The generated instructions emphasize reproducibility, datasets, metrics, ablations, and clear experiment notes.
