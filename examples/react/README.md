# React Example

Generate a React-focused agent setup:

```bash
agent-set init --profile react --skills frontend-polish,code-review,test-writer --output .
```

The generated instructions emphasize responsive UI, accessibility, visual consistency, and build verification.
